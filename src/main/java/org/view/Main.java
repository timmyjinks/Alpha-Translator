package org.view;

import org.controller.Controller;

public class Main {
    public static void main(String[] args) {
        Controller controller = new Controller();
        controller.start();
    }

    // Rewrite the Io class for getting strings and int
    // Write a menu with 4 to 5 options and one sub-menu
    // create an abstract class with 3-4 concrete classes
    // Instantiate, fill, and display the classes
}